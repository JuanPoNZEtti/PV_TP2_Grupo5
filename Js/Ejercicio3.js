// Calcular el volumen de una esfera que tiene como radio 4 metros.

let radio = 4;
const pi = 3.141592 ;

let volumen = (4/3) * pi * Math.pow(radio, 3);

console.log(`El volumen de una esfera con radio de 4 metros es: ${volumen.toFixed(2)} metros`);